var multiplyRecursive = function(array) {
  if (array.length === 1)
    return array[0];
  else {
    return (array.pop() * multiplyRecursive(array));
  }
};

console.log(multiplyRecursive([4,54,10]));
