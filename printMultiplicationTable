function printTable() {
        var bigTable = [];
        for ( j = 1 ; j <= 12 ; j++ ) {
          var table = [];
            for ( k = 1 ; k <= 12 ; k++ ) {
              table.push(j*k);
            }
          bigTable.push(table);
        }
  return bigTable;
}

console.log(printTable());
